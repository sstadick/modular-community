context:
  version: "0.8.0"
  mojo_version: ">=1.0.0, <1.1.0"

package:
  name: "argmojo"
  version: ${{ version }}

source:
  - git: https://github.com/forfudan/argmojo.git
    rev: 2ba77c1be364e49fe7db88c724dd0f9a25ed3a44

build:
  number: 0
  script:
    - mkdir -p ${PREFIX}/lib/mojo
    - mojo precompile src/argmojo -o ${{ PREFIX }}/lib/mojo/argmojo.mojoc

requirements:
  host:
    - mojo-compiler ${{ mojo_version }}
  build:
    - mojo-compiler ${{ mojo_version }}
  run:
    - mojo-compiler ${{ mojo_version }}

about:
  homepage: https://github.com/forfudan/argmojo
  license: Apache-2.0
  license_file: LICENSE
  summary: A feature-rich command-line argument parser library for Mojo, with both builder and struct-based declarative APIs.
  repository: https://github.com/forfudan/argmojo.git

extra:
  project_name: argmojo
  maintainers:
    - forfudan
